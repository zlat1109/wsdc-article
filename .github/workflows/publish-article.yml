name: Publish Article to Telegraph

on:
  workflow_dispatch:
    inputs:
      article_file:
        description: 'Path to the article Markdown file'
        required: true
        default: 'article_draft.md'
      telegram_chat_id:
        description: 'Target Telegram Chat ID (e.g., @channelname or -100...)'
        required: true
        default: ''

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip list

      - name: Publish Article
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: |
          python3 publish_article_bot.py \
            --file "${{ inputs.article_file }}" \
            --chat-id "${{ inputs.telegram_chat_id }}"
